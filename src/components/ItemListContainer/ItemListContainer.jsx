export const ItemListContainer = ({titulo, productos}) => {
    // estado

    //llamado a la base de datos

    return (
        <section>
            <h1>{titulo}</h1>
        {
            
        }
        </section>
                 
    );
};