import "./Cart.css";

export const Cart = () => {
    const { cart, clearCart, deleteItem, toetalPrica, checkout } = useCartContext();

    return <section className="item-list-conteinar">
        <h2>Carrito de compras</h2>

        

        ):(

        <p>Tu carrito está vacio</p>
        )}

    </item>)
}
            
        
    </section >
}
